# WarDogs 游戏工具 — 开发协作规范

## 项目目标与当前阶段

- 构建面向 WarDogs 玩家的游戏工具主题网站，兼容手机、平板和桌面端。
- 首个工具为「迫击炮位置方位计算器」，后续工具应能独立扩展。
- 生产环境部署于 Cloudflare Workers，架构、依赖、存储和交付流程必须兼容该平台。
- 脚手架已落地：React + Vite + TypeScript 前端、Workers Static Assets 托管、Vitest/Playwright 测试与 GitHub Actions 流水线均已就位；`worker/`、`migrations/` 等目录按需求出现时再创建。下文目录、技术选型和命令为现行约定，改动时同步更新本节与 README。
- 默认使用简体中文沟通、编写项目文档和界面文案；代码标识符使用清晰的英文。

## 技术选型与平台约束

- 默认采用 TypeScript（严格模式）+ React + Vite 构建前端，使用 Workers Static Assets 托管构建产物；服务端功能按需添加至 Worker。
- 使用 Wrangler 管理本地开发、环境绑定和部署，以 `wrangler.jsonc` 作为部署配置来源。脚手架落地时核对 Cloudflare 官方文档及依赖兼容性，固定兼容日期。
- 默认使用 pnpm，提交唯一锁文件并固定包管理器版本；本地和 CI 使用一致的 Node.js 与包管理器版本。Node.js 用于构建和工具链，不作为生产服务运行前提。
- 优先使用 Web 标准 API。不得依赖常驻 Node.js 服务、可持久化本地磁盘或仅适用于传统服务器的运行机制；确需 Node.js API 时逐项确认 Workers 支持情况及兼容标志。
- 存储按需求选用，不为简单计算器提前创建云资源：
  - D1：需要关系查询和持久化的结构化数据；数据库变更必须以版本化迁移管理。
  - Workers KV：配置、缓存等可接受最终一致性的键值数据；需要强一致性的业务状态不能依赖 KV。
  - R2：地图、图片等大体积对象；普通前端构建资源优先使用 Static Assets。
  - 浏览器本地存储：用户偏好和无需跨设备同步的少量历史记录。
- 云资源通过 Worker bindings 访问；不得将凭据或服务端绑定暴露至前端。开发、测试和生产的数据资源必须隔离。
- 引入依赖应有明确用途，并检查 Workers 兼容性、维护情况及前端体积；不预先引入账户、后台管理或复杂服务架构。

## 首个工具：迫击炮位置方位计算器

### 首版范围

- 支持输入游戏内迫击炮位置与目标位置，计算两点距离和从迫击炮指向目标的方位角。
- 明确显示坐标格式、距离单位、方位角单位、方向基准和计算结果；支持修改输入、重新计算和重置。
- 核心计算优先在浏览器完成，不依赖网络请求；计算模块必须与界面、地图组件及存储解耦。
- 地图选点、网格坐标解析、记录分享等按后续需求扩展；地图交互应保留可直接输入坐标的替代入口。
- 仰角、密位、装药和射程提示仅在获得可验证的游戏机制或射表后实现，不把通用弹道公式当作游戏实际机制。

### 数据与计算正确性

- 实现前核实游戏版本、地图比例尺、坐标原点、轴方向、网格编码和方位角显示规则；记录来源与适用版本，不猜测游戏专有参数。
- 缺少游戏数据时，可推进界面、通用二维几何模块和测试夹具；示例数据必须标注，不能伪装成已验证的游戏数据。
- 在坐标适配层统一游戏坐标与屏幕坐标，明确处理屏幕 Y 轴向下的情况；距离转换必须使用已验证的地图比例尺。
- 内部方位角默认约定为正北 0°、顺时针递增，归一化到 `[0, 360)`；若游戏规则不同，在适配层转换并在界面说明。
- 中间计算保留精度，仅在显示阶段舍入；显示舍入后的角度也应保持范围有效。
- 校验空值、非数字、非有限数、坐标范围及比例尺；同点应返回距离为零、方位无定义，不能展示误导性的有效方向。
- 计算测试至少覆盖正北、正东、正南、正西、四象限、同点、轴翻转、比例尺换算、角度归一化和非法输入；使用独立推导的预期结果。

## 移动端与交互要求

- 采用移动端优先的响应式布局，至少检查 320px、375px、768px 和桌面宽度；页面不得出现非预期的横向溢出。
- 核心输入和结果在小屏上清晰可用，触控目标尽量不小于 44 × 44 CSS 像素；功能不能只依赖 hover 或鼠标操作。
- 输入框使用明确标签和合适的移动端键盘类型，同时兼容合法的小数、负数等坐标格式。
- 支持键盘操作、可见焦点、合理对比度和可被辅助技术识别的错误提示；不得仅用颜色区分位置或状态。
- 如果加入地图，验证触控缩放、拖动与页面滚动的关系，并保证横竖屏切换后坐标和标记正确。
- 优先保证计算操作流畅；大地图与非首屏资源按需加载，避免高频交互重复触发昂贵计算。

## 代码组织与质量

建议在初始化脚手架时采用以下结构，按实际功能创建目录，不生成空壳模块：

```text
src/
  app/                  # 应用入口、路由与页面布局
  components/           # 跨工具复用的界面组件
  tools/mortar/         # 迫击炮工具界面、坐标适配与纯计算逻辑
  lib/                  # 确有跨模块用途的公共逻辑
worker/                 # 按需添加的 Worker 入口与 API
public/                 # 随站点发布的静态资源
migrations/             # 引入 D1 后的数据库迁移
tests/                  # 集成与端到端测试；单元测试可与模块同目录
.github/workflows/      # CI 与发布部署流程
wrangler.jsonc          # Workers 配置
```

- 保持工具模块边界清晰，避免将游戏规则、坐标换算和组件状态混在一起。
- 禁止以宽泛的 `any` 或忽略类型错误绕过建模；外部输入在进入业务逻辑前校验。
- 计算单元测试默认使用 Vitest；关键用户流程使用 Playwright。涉及 bindings 的服务端测试需覆盖 Workers 运行环境。
- 日志应便于定位错误，不记录密钥或不必要的用户输入；API 返回明确的错误状态和可理解的提示。
- 修改计算逻辑必须验证相应边界；修改界面应检查移动端和桌面端。仅修改文档时检查准确性和格式，无需运行无关测试。

## 自动化发版与部署

- 默认使用 GitHub Actions + Wrangler。若最终代码托管平台不同，保留同等检查和发布能力；不要同时启用会重复发布的部署流水线。
- 建立统一的 package scripts：`dev`、`build`、`lint`、`typecheck`、`test`、`test:e2e`、`deploy:staging`、`deploy:production`。这些命令需在脚手架落地后实现，并同步写入 README。
- PR 执行锁文件冻结安装、lint、类型检查、相关测试和生产构建；Worker 代码及配置还需通过 Wrangler 部署预检。
- 默认将 `main` 通过检查后的提交自动部署到 staging；以 `vX.Y.Z` 版本标签触发生产发布，验证版本号匹配且标签提交已进入 `main`。
- 发版流程应自动生成发布说明，记录版本、提交 SHA 和部署标识；生产部署必须通过相同质量检查，同一环境的部署串行执行，避免旧版本覆盖新版本。
- 在首次搭建流水线时完成 GitHub、Cloudflare 账户、环境、资源绑定和域名配置说明；不得臆造账户 ID、资源 ID 或已部署地址。
- 使用最小权限的 Cloudflare API Token，保存在 CI secrets 中；应用密钥使用 Workers secrets。不得提交 `.env`、`.dev.vars`、令牌或其他凭据，可提供不含真实值的示例文件。
- 预览与 staging 不得连接生产数据；来自不受信任 PR 的代码不得获得部署密钥。
- 如使用 D1，先在测试环境验证迁移；生产迁移优先保持与旧版应用兼容，并在部署流程明确执行顺序。避免破坏性迁移与应用发布强耦合。
- 部署后执行站点可访问性及关键计算流程冒烟检查；发布失败时保留诊断信息。文档必须说明应用版本回滚和数据恢复的区别，应用回滚不能视为数据库自动回滚。

## 协作约定

- 开始任务先阅读本文件及相关代码，以用户当前指令为准；新建或调整目录时保持规范与实际结构一致。
- 对范围清晰的工作直接推进；涉及尚未确定的游戏规则时指出缺失信息，并继续完成不受影响的部分。
- 保持改动聚焦，不覆盖用户已有改动，不顺手重构无关模块；仅在用户要求时提交代码。
- 不将规划中的功能、命令、测试或部署描述为已经完成；交付时说明实际变更、验证结果与剩余限制。
- 引入或升级 Cloudflare 配置及依赖前核对官方文档，避免照搬过期 API 或不适配 Workers 的模板。

## 参考资料

- [Cloudflare Workers Static Assets](https://developers.cloudflare.com/workers/static-assets/)
- [通过 GitHub Actions 部署 Workers](https://developers.cloudflare.com/workers/ci-cd/external-cicd/github-actions/)
- [Cloudflare D1](https://developers.cloudflare.com/d1/)
- [Workers KV](https://developers.cloudflare.com/kv/)
- [Cloudflare R2](https://developers.cloudflare.com/r2/)
